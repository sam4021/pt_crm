<div class="row">
  <div class="col-sm-12">
    <h4>Edit Product IMEI</h4>
  </div>
  <form class="" action="/vendor/collected/edit/<%= cprodI.id %>" method="post">
  <div class="col-sm-12">
      <% for( var cprod of cproducts){
          if(cprod.id == cprodI.collected){
             for( var prod of products){
               if(cprod.product == prod.id){
        %>
          <label class="pull-left">Title :: <%= prod.title %></label>
        <% } } } } %>
  </div>
        <div class="col-sm-12">
          <table class="table table-bordered">
            <thead>
              <tr>
                <th>IMEI</th>
              </tr>
            </thead>
            <tbody>
                <tr>
                  <td><input type="text" name="imei" value="<%= cprodI.imei %>"></td>
                </tr>
            </tbody>
          </table>
        </div>
        <input type="hidden" name="id" value="<%= cprodI.collected %>">

      <div class="clearfix"></div>
      <div class="form-group col-sm-12 padding-15">
        <button type="submit" class="btn btn-success">Submit</button>
      </div>
    </form>
</div>
